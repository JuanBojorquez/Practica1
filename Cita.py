#!/usr/bin/python3
import lector
import argparse

def contar(archivo,archivo_sw):
    texto=lector.leer_archivo(archivo)
    
    
    
def main(archivo,archivo_sw):
  texto=lector.leer_archivo(archivo)
  sp=lector.leer_stopwords(archivo)
  contar(texto,sw)